{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm turbo run build --filter=@sharelyai/webcontrol... --filter=@sharelyai/demo... && rm -rf dist && cp -r apps/demo/dist dist && cp -r apps/webcontrol/dist/assets/. dist/assets/",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
